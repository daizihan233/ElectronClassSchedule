{
  "name": "electron_class_schedule",
  "version": "0.0.0",
  "description": "classSchedule",
  "main": "main.js",
  "author": "KuoHu",
  "license": "GPLv3",
  "scripts": {
    "debug": "chcp 65001 && electron . --enable-logging",
    "rebuild": "electron-rebuild",
    "build": "electron-builder --win --x64",
    "dist": "electron-builder --win --x64",
    "postinstall": "electron-builder install-app-deps"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0",
    "electron": "^22.3.27",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "electron-disable-minimize": "^2.1.0",
    "electron-prompt": "^1.7.0",
    "electron-store": "^8.1.0",
    "electron-updater": "^6.3.9",
    "node-gyp": "^10.0.1",
    "fstream": "1.0.12",
    "windows-shortcuts": "^0.1.6",
    "ws": "^8.18.0"
  },
  "build": {
    "appId": "cn.khbit.classschedule",
    "productName": "电子课表",
    "asar": true,
    "directories": {
      "buildResources": "build-resources"
    },
    "files": [
      "**/*",
      "!out/**",
      "!dist/**",
      "!**/*.map",
      "!**/*.log"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "电子课表",
      "artifactName": "ElectronClassSchedule-Setup-${version}.${ext}"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "daizihan233",
        "repo": "ElectronClassSchedule",
        "releaseType": "release"
      }
    ]
  }
}
